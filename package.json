{
  "name": "wp-resource-search",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node bootstrap.mjs",
    "dev": "node --watch bootstrap.mjs",
    "check": "node --check bootstrap.mjs && node --check server.mjs && node --check app.js && node --check search.js && node --check admin.js && node --check admin-persistence.js && node --check admin-import.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
